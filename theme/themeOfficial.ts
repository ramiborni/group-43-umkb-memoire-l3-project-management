import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  palette: {
    //type: 'dark',
    primary: {
      main: '#736eae',
    },
  },
  typography: {
    fontFamily: 'poppins',
  },
});

export default theme;
